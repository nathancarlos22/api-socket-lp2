# api-socket-lp2

Para executar o projeto, abra o zap-raiz-v2.exe, cadastre e faça login. E então converse em grupo.

OBS: É preciso ter o arquivo .exe da api do socket na mesma pasta do zap-raiz-v2 para então executar o zap-raiz-v2
